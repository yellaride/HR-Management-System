# TODO: Sidebar/labels professional rename for attendance

- [ ] Update admin sidebar entry: change label "Employee Attendance" -> "Attendance Record" (and ensure icon is distinct/professional)
- [ ] Ensure the other related buttons/entries (attendance vs settings) don’t reuse the same icon; update only the sidebar icons that are wrong.
- [ ] Update page headings/subtitles if they are inconsistent with the new naming (e.g., “On-Site Attendance” -> “Attendance Record” where appropriate).
- [ ] Run typecheck/lint/build (or at least `npm run lint`) to confirm no TS/React errors.

